#ifndef READ_INPUT_H
#define READ_INPUT_H

#include "comms.h"
#include "definitions.h"

#include <fstream>

void read_input(std::ifstream& g_in, parallel_& parallel, global_variables& globals);

#endif

